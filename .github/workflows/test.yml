name: test

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  test:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - run: gleam deps download
      - run: gleam test
      - run: gleam format --check src test
